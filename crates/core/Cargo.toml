[package]
name = "ticketsystem-core"
version = "0.1.0"
edition = "2024"

[features]
default = ["lang-en"]
lang-en = []
lang-de = []
lang-es = []

[dependencies]
serde = { version = "1", features = ["derive"] }
